#include "main.h"
#Include <string.h>

char *_strcat(char *dest, char *src)
{
	dest_len  = strlen(dest);
	n = strlen(src)
       for (i=0; i<n && src[i] != '\0' ; i++)
		dest[dest_len + i] = src[i];
 	dest[dest_len + + i] = src[i];
	
	return 	dest;
}

